cd C:\xampp\htdocs\jtikrooms
Get-Content .\storage\logs\laravel.log -Tail 80 -Waitname: Pull Requests

on:
  pull_request_target:
    types: [opened]

permissions:
  pull-requests: write

jobs:
  uneditable:
    uses: laravel/.github/.github/workflows/pull-requests.yml@main
